import styled from 'styled-components';
import { theme } from '../../constants/theme';

export const HrCustomContainer = styled.hr`
  margin: 64px;
  background: ${theme.APP.theme_gradient};
  border: none;
  height: 2px;
`;
